# Decisions

- Updated both `AGENTS.md` and `frontend/AGENTS.md` so the root guidance stays truthful and the nested frontend guidance reflects the implemented route-group/feature-slice architecture.
- Kept backend conventions aligned with nested backend AGENTS files and did not rewrite backend nested docs.
